<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abdullah.books">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.abdullah.books.ui.home.fragment.ProductDetailsActivity" />
        <!-- <activity -->
        <!-- android:name=".RegisterActivity" -->
        <!-- tools:ignore="LockedOrientationActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <activity android:name="com.abdullah.books.ui.home.activity_.OrderDetailsActivity" />
        <activity android:name="com.abdullah.books.ui.home.activity_.MyAddressesActivity" />
        <activity android:name="com.abdullah.books.ui.home.activity_.DeliveryActivity" />
        <activity android:name="com.abdullah.books.ui.home.activity_.AddAddressActivity" />
        <activity android:name=".ui.home.activity_.MainActivity" />
        <activity android:name=".ui.home.activity_.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.activity_.CategoryActivity" />
    </application>

</manifest>